#include <stdio.h>
#include <iostream>
#include <vector>

using namespace std;

typedef long long int llint;

llint solve(llint A, llint B)
{
	// SCRIVI QUA IL TUO CODICE
	return (0);
}

int main()
{
	llint T;
	cin >> T;

	while (T > 0)
	{
		llint A;
		llint B;
		cin >> A;
		cin >> B;

		cout << solve(A, B) << endl;
		--T;
	}
	return (0);
}
